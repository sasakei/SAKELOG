<!-- トップページ -->
<% if logged_in? %>
  <div class="container">
    <div class="row">
      <div class="micropost-index col-md-8 col-md-offset-2">
        <h3>投稿一覧</h3>
        <div class="microposts-index-wrapper">
          <%= render 'shared/feed' %>
        </div>
      </div>
    </div>
  </div>
<% else %>
    <div class="container top-page-wrapper">
      <div class="row">
        <div class="col-md-6 col-md-offset-3 copy">
          <%= link_to( guest_sign_in_path, method: :post, class: "btn-flat-border" )  do%>
            <h3><i class="fas fa-door-open fa-x2"></i>お試しログイン</h3>
          <% end %>
        </div>
      </div>
    </div>
<% end %>
