<div class="container">
  <div class="row">
    <h3><%= @title %></h3>
    <div class="users-index col-md-6 col-md-offset-3">
      <% if @users.any? %>
        <%= will_paginate %>
        <ul class="users follow">
          <%= render @users %>
        </ul>
        <%= will_paginate %>
      <% end %>
    </div>
  </div>
</div>
